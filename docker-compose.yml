services:
  mongo:
    image: mongo:7
    container_name: verbovivo-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - verbovivo-mongo-data:/data/db

volumes:
  verbovivo-mongo-data:
